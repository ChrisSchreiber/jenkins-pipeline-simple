pipeline {
    agent none
    stages {
        stage('Verify') {
            steps {
                echo "verify"
            }
        }
        stage('Deploy') {
            steps {
                echo "deploy"
            }
        }
    }
}

